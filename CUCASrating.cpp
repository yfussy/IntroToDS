#include <iostream>
#include <vector>
#include <set>
#include <iomanip>

using namespace std;

int main() {
    cout << std::fixed << std::setprecision(2);
    int n, id, rating;
    string prof;
    for (int i = 0; i < n; i++) {
        cin >> id >> prof >> rating;
        
    }
}